<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cipher Lounge - Where Encrypted Minds Gather</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 via-black to-gray-900 min-h-screen text-white flex flex-col">

  <!-- NAVBAR -->
<%- include('/partials/header.ejs') %>

  <!-- HERO -->
  <section class="text-center mt-20 px-6 relative">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[400px] bg-green-500/20 rounded-full blur-3xl"></div>

    <h1 class="text-5xl md:text-6xl font-extrabold mb-6 relative z-10 hero-title">
      Welcome to the<br/>
      <span class="text-green-400">Cipher Lounge</span>
    </h1>
    <p class="text-gray-400 max-w-2xl mx-auto mb-10 relative z-10 hero-sub">
      Speak freely, stay anonymous.<br>
      Only those who crack the cipher may reveal the true identities.
    </p>
    <a href="/signup" class="px-10 py-4 bg-green-500/90 text-white font-bold rounded-full hover:bg-green-400 transition relative z-10 hero-btn">
      Sneak Peek→
    </a>
  </section>

  <!-- FEATURE CARDS -->
  <section class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto mt-32 px-6 feat-cards">
    <div class="bg-white/5 border border-green-400/20 rounded-2xl shadow-lg p-8 text-center hover:border-green-400 transition">
      <span class="text-4xl">🧠</span>
      <h3 class="font-bold text-xl mt-4 mb-2 text-green-400">Encrypted Threads</h3>
      <p class="text-gray-400">Every message is cloaked — only members can see who said what.</p>
    </div>
    <div class="bg-white/5 border border-green-400/20 rounded-2xl shadow-lg p-8 text-center hover:border-green-400 transition">
      <span class="text-4xl">🔒</span>
      <h3 class="font-bold text-xl mt-4 mb-2 text-green-400">Secure Membership</h3>
      <p class="text-gray-400">Access requires a secret passcode. No entry without clearance.</p>
    </div>
    <div class="bg-white/5 border border-green-400/20 rounded-2xl shadow-lg p-8 text-center hover:border-green-400 transition">
      <span class="text-4xl">🛡️</span>
      <h3 class="font-bold text-xl mt-4 mb-2 text-green-400">Admin Override</h3>
      <p class="text-gray-400">Admins have eyes on everything — and the power to purge the chatter.</p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="text-center mt-20 pb-10 text-gray-500 border-t border-white/10 pt-10">
    © 2025 Cipher Lounge. All access logged. All rights encrypted.
  </footer>

  <!-- GSAP -->
  <script>
    gsap.fromTo(".hero-title", { x: -1000 }, { x: 0, duration: 1, ease:"power1.out" });
    gsap.fromTo(".hero-sub",{opacity:0,y:80},{opacity:1,y:0,duration:1,delay:0.3,ease:"power4.out"});
    gsap.fromTo(".hero-btn",{opacity:0,scale:0.8},{opacity:1,scale:1,duration:0.8,delay:0.7,ease:"back.out(1.7)"});
    gsap.fromTo(".feat-cards", { opacity: 0, y: 120, scale: 0.95 }, { opacity: 1, y: 0, scale: 1, duration: 1.2, ease: "elastic.out(1, 0.6)", delay: 0.4 });
  </script>
</body>
</html>
